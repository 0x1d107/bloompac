const proxydata = "SOCKS5 127.0.0.1:1080";
const ctset = [
#include "ctset.json"
][0];
function FindProxyForURL(url,host){
alert("Proxy for url:"+url);
const PROXY = proxydata+"; DIRECT"; 
var tld = host.split('.').slice(-1)[0]
var hostpriv = host.split('.').slice(-2).join('.')
var hostpriv3 = host.split('.').slice(-3).join('.')
if(ctset[host]||ctset[hostpriv]||ctset[hostpriv3]){
	alert("Blocked domain. Proxying through "+proxydata)
	return PROXY;
}
alert("Unblocked domain.");
return "DIRECT";
}
